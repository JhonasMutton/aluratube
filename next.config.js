/** @type {import('next').NextConfig} */

const nextConfig = {
    reactStrictMode: true,
    compiler: {
      styledComponents: true, // Indica que vai usar o compilador do Styled Components para ajudar.
    },
  }
  
  module.exports = nextConfig